import { readFile, existsSync, readFileSync, createReadStream } from 'fs';
import path from 'path';

export async function load({ params }) {
	const playerId = params.playerId;
	const imageId = params.imageId;

	return { file };
}
